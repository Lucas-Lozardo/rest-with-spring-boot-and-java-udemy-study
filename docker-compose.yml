services:
  mysql:
    image: mysql:9.1.0
    container_name: mysql-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: rest_with_spring_boot_and_java
      MYSQL_ROOT_PASSWORD: root_pass
      MYSQL_USER: app_user
      MYSQL_PASSWORD: app_pass
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
